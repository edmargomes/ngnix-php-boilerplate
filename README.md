# ngnix-php-boilerplate
This project works with Ansible, Docker and Vagrant to create automated servers with php and nginx.

## Install

ansible-galaxy install -r requirements.txt -p roles --ignore-errors
